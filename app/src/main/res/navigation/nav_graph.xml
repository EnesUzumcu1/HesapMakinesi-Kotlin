<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/savedCoinsFragment">

    <fragment
        android:id="@+id/savedCoinsFragment"
        android:name="com.example.hesapmakinesi.ui.savedcoins.SavedCoinsFragment"
        android:label="fragment_saved_coins"
        tools:layout="@layout/fragment_saved_coins" >
        <action
            android:id="@+id/action_savedCoinsFragment_to_calculateFragment"
            app:destination="@id/calculateFragment" />
    </fragment>
    <fragment
        android:id="@+id/calculateFragment"
        android:name="com.example.hesapmakinesi.ui.buyandsell.BuyAndSellFragment"
        android:label="fragment_calculate"
        tools:layout="@layout/fragment_buyandsell" />
</navigation>